# AI For You — Local Refresh Skill (Renamed)

This skill has been renamed to `ai-for-you-sync`.

Use `/ai-for-you-sync [post_number]` going forward.

The sync skill also responds to `/ai-for-you-refresh` as a legacy alias, so existing muscle memory still works.
